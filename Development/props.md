-----------------REACT-PROPS------------------------


In React, props (short for "properties") are read-only arguments passed into components to transfer data from a parent component to a child component. They work exactly like HTML attributes and serve as the primary mechanism for making components dynamic and reusable.



import Reacxt from 'react'

function App(){
    return(
    <div>
    <h2>Hello Aayush</h2>

    </div>

    );
};

export defaut App;